package eigerfix;

public class RfqOrder
{
	// Java doesn't support struct
	// A struct is a value type on the stack, a class would use stack pointers referencing the values on the heap

	public String order;
	public String client;	
}

